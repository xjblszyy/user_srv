// Code generated with goa v3.0.2, DO NOT EDIT.
//
// userProfile protocol buffer definition
//
// Command:
// $ goa gen user/design

syntax = "proto3";

package user_profile;

option go_package = "user_profilepb";

// The userProfile service makes it possible to view, add or remove user info.
service UserProfile {
	// Show userProfile by Token
	rpc Retrieve (RetrieveRequest) returns (RetrieveResponse);
	// Add new user and return its ID.
	rpc Create (CreateRequest) returns (CreateResponse);
	// Creates a valid JWT
	rpc Signin (SigninRequest) returns (SigninResponse);
	// Update avatar and nickname about user
	rpc Update (UpdateRequest) returns (UpdateResponse);
}

message RetrieveNotFoundError {
	// Message of error
	string message_ = 1;
	// ID of missing user
	string id = 2;
}

message RetrieveInvalideTokenError {
	// Message of error
	string message_ = 1;
	// ID of missing user
	string id = 2;
}

message RetrieveRequest {
}

message RetrieveResponse {
	// ID is the unique id of the userProfile.
	string id = 6;
	// email of userProfile
	string email = 1;
	// password of userProfile
	string password = 2;
	string avator = 4;
	string nickname = 5;
}

message CreateEmailExistError {
	// Message of error
	string message_ = 1;
	// ID of missing user
	string id = 2;
}

message CreateRequest {
	// email of userProfile
	string email = 1;
	// password of userProfile
	string password = 2;
}

message CreateResponse {
	string field = 1;
}

message SigninEmailOrPasswordErrorError {
	// Message of error
	string message_ = 1;
	// ID of missing user
	string id = 2;
}

message SigninRequest {
	// Email used to perform signin
	string email = 1;
	// Password used to perform signin
	string password = 2;
}

message SigninResponse {
	string field = 1;
}

message UpdateInvalideTokenError {
	// Message of error
	string message_ = 1;
	// ID of missing user
	string id = 2;
}

message UpdateRequest {
	// avator of userProfile
	string avator = 1;
	// nickname of userProfile
	string nickname = 2;
}

message UpdateResponse {
}
